import {Link} from 'react-router-dom';import {ArrowRight,CalendarDays,MessageCircle} from 'lucide-react';import type {ReactNode} from 'react';
export function Button({to,children,variant='primary',external=false}:{to:string;children:ReactNode;variant?:'primary'|'outline'|'light';external?:boolean}){const cls=`inline-flex items-center justify-center gap-2 rounded-full px-6 py-3.5 text-sm font-semibold transition duration-300 hover:-translate-y-0.5 ${variant==='primary'?'bg-ecp-blue text-white shadow-lg shadow-blue-900/20 hover:bg-ecp-cobalt':variant==='outline'?'border border-ecp-blue text-ecp-blue hover:bg-blue-50':'bg-white text-ecp-navy hover:bg-blue-50'}`;const icon=children?.toString().toLowerCase().includes('whatsapp')?<MessageCircle size={18}/>:children?.toString().toLowerCase().includes('evaluación')?<CalendarDays size={18}/>:<ArrowRight size={18}/>;return external?<a className={cls} href={to} target="_blank" rel="noreferrer">{icon}{children}</a>:<Link className={cls} to={to}>{icon}{children}</Link>}
